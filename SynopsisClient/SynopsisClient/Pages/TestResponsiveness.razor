@page "/tests"
@inject SynopsisClient.Model.SynopsisHandler Handler

<h1>Test Responsiveness</h1>

<EditForm EditContext="@Handler.CurrentEditContext">

    @foreach (var author in Handler.Synopsis.Authors)
    {
        <div>
            <span>
                <label title="Author's name">
                    <span class="form-label">Name</span>
                    <br />
                    <InputText @bind-Value="author.Name" id="name" />
                </label>
            </span>
            <span>
                <label title="Author's Email address">
                    <span class="form-label">Email</span>
                    <br />
                    <InputText @bind-Value="author.Email" id="name" />
                </label>
            </span>
            <span>
                <label title="Author's GitHub username">
                    <span class="form-label">GitHub</span>
                    <br />
                    <InputText @bind-Value="author.GitHub" id="name" />
                </label>
            </span>
            <span>
                <label title="Author's Twitter username">
                    <span class="form-label">Twitter</span>
                    <br />
                    <InputText @bind-Value="author.Twitter" id="name" />
                </label>
            </span>
        </div>

        <hr />
    }


    @*<table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>GitHub</th>
                <th>Twitter</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var author in Handler.Synopsis.Authors)
            {
                <tr>
                    <td><InputText @bind-Value="author.Name" id="name" /></td>
                    <td><InputText @bind-Value="author.Email" id="email" /></td>
                    <td><InputText @bind-Value="author.GitHub" id="github" /></td>
                    <td><InputText @bind-Value="author.Twitter" id="twitter" /></td>
                    <td><button @onclick="@(e => Handler.Delete(author))">Delete</button></td>
                </tr>
            }
        </tbody>
    </table>*@

    <div class="bottom-buttons-area" style="margin-bottom: 10px;">
        <button @onclick="Handler.AddItem">Add an author</button>
    </div>

    <div class="bottom-buttons-area">
        <button disabled="@Handler.CannotSave" @onclick="Handler.CheckSaveSynopsis">Save local</button>
        <button @onclick="Handler.ReloadFromCloud">Reload from cloud</button>
    </div>
</EditForm>

@code
{ 
    protected override async  Task OnInitializedAsync()
    {
        await Handler.InitializePage();
        Handler.DefineList(Handler.Synopsis.Authors);
    }

}